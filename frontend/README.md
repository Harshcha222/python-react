Flask Library Frontend

Quick start:

1. Install dependencies

```bash
cd frontend
npm install
```

2. Run dev server

```bash
npm run dev
```

Notes:
- The frontend expects the Flask backend on `http://localhost:5000`.
- If you see CORS errors, enable CORS in the Flask app (e.g., `flask_cors`).
